<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".App.MyApp"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EcoLinkHubApp"
        tools:targetApi="31">
        <activity android:name=".MarcheBioActivity" />
        <activity android:name=".MenuActivity" />


        <activity android:name=".ModifierInitiativeActivity" />
        <activity android:name=".AjouterInitiativeActivity" />
        <activity android:name=".Marche_initActivity" />
        <activity android:name=".Dechet_initActivity" />
        <activity android:name=".Eau_initActivity" />
        <activity android:name=".Energie_initActivity" />
        <activity android:name=".Consommation_initActivity" />
        <activity android:name=".Covoiturage_initActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="VOTRE_CLE_API" />
    </application>

</manifest>